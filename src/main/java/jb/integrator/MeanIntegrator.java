package jb.integrator;

import jb.config.Opts;
import jb.data.SelectedTuple;

public class MeanIntegrator implements Integrator {
    @Override
    public double integrateScore(SelectedTuple selectedTuple, Opts opts) {
        return 0;
    }
}
